apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kube-image-guardian.fullname" . }}-test-connection"
  labels:
    {{- include "kube-image-guardian.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kube-image-guardian.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
